<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsf.stdntapt.dao.SpeciDao">
    <select id="findSpeciAll" parameterType="int" resultType="Speciality">
		select speci_id as speciId, speci_name as speciName, college_id as collegeId, spe_years_id as speYearsId from speciality where deleted=0
    </select>
</mapper>